import CalendarToolsPage from "@/components/CalendarToolsPage";

// Calendar feed and export controls, also reachable from the timetable header.
export default function Page() {
	return <CalendarToolsPage />;
}
