import EventsManagerPage from "@/components/EventsManagerPage";

// Scheduled-event cleanup, also reachable from the timetable header.
export default function Page() {
	return <EventsManagerPage />;
}
